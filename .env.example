# Environment Variables
# Copy this file to .env and fill in your actual values

# Supabase (browser client: native Supabase Auth - email/password + Google)
# The anon/public key is safe to expose client-side; it relies on RLS.
#   VITE_SUPABASE_URL      : Supabase dashboard, Settings, API, Project URL
#   VITE_SUPABASE_ANON_KEY : Supabase dashboard, Settings, API, anon public key
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_public_key_here

# Firebase Configuration (legacy; auth has moved to Supabase)
VITE_FIREBASE_API_KEY=your_firebase_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_firebase_auth_domain_here
VITE_FIREBASE_PROJECT_ID=your_firebase_project_id_here
VITE_FIREBASE_STORAGE_BUCKET=your_firebase_storage_bucket_here
VITE_FIREBASE_MESSAGING_SENDER_ID=your_firebase_messaging_sender_id_here
VITE_FIREBASE_APP_ID=your_firebase_app_id_here

# Stripe Configuration
VITE_STRIPE_PUBLIC_KEY=your_stripe_public_key_here
# Server-only (Supabase secrets — never VITE_):
#   STRIPE_SECRET_KEY=
#   STRIPE_WEBHOOK_SECRET=
#   STRIPE_IMPACT_LOG_PRICE_ID=price_xxx   # $5/mo Impact Log Pro (1-month access cycle)
#   STRIPE_FULL_PROGRAMME_PRICE_ID=price_xxx  # $50/year Full Programme (12 months)
#   APP_BASE_URL=https://app.t4leader.com

# App Configuration
VITE_APP_BASE_URL=https://app.t4leader.com

# Admin Bootstrap
# A comma-separated list of emails for users who should be granted Super Admin role on their first login.
# Example: VITE_BOOTSTRAP_ADMIN_EMAILS=admin1@example.com,admin2@example.com
VITE_BOOTSTRAP_ADMIN_EMAILS=

# Enable real-time profile updates for onSnapshot listeners (e.g., AuthContext, partner dashboards, and leaderboard views).
# Set to "true" to keep organization assignments synchronized without manual refresh.
VITE_ENABLE_PROFILE_REALTIME=true

# Feature Flags
# Enable parallel tracking for the new window-based progress system.
# When true, the app will write to both weeklyProgress and windowProgress collections.
VITE_FEATURE_FLAG_PARALLEL_WINDOW_TRACKING=false

# SurveyMonkey API (server-side only — NEVER use a VITE_ prefix)
# From: https://developer.surveymonkey.com/apps/ → T4lAPI → SETTINGS
# Put the real values in your local `.env` (gitignored), not in chat.
# Do NOT add these to Vercel as VITE_* (that would expose them in the browser).
# For production: supabase secrets set SURVEYMONKEY_ACCESS_TOKEN=...
SURVEYMONKEY_ACCESS_TOKEN=your_surveymonkey_access_token_here
SURVEYMONKEY_CLIENT_ID=your_surveymonkey_client_id_here
SURVEYMONKEY_CLIENT_SECRET=your_surveymonkey_client_secret_here
